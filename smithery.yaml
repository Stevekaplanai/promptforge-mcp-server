name: promptforge-mcp
description: AI-powered prompt optimization MCP server
runtime: container
build:
  dockerfile: Dockerfile
config:
  transport:
    type: http
    port: 8000
    host: 0.0.0.0
  environment:
    NODE_ENV: production
    PORT: "8000"